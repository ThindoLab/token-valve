{
  "name": "@tokenvalve/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "tokenvalve": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src",
    "start": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tokenvalve/core": "workspace:*",
    "@tokenvalve/dashboard": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
