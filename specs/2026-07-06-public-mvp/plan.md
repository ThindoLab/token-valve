# Plan: Public MVP

## Group 1 — 根 README

1.1 写清 TokenValve 的定位和边界。

1.2 写 macOS 安装、构建、doctor 验证。

1.3 写快速开始闭环。

---

## Group 2 — Provider 示例

2.1 添加 GitHub 示例。

2.2 添加 Supabase 示例。

2.3 添加 Vercel 示例。

2.4 添加 LLM key 示例。

2.5 添加 custom provider 示例。

---

## Group 3 — Agent 集成文档

3.1 添加 MCP client 配置说明。

3.2 添加 Skill onboarding 示例。

3.3 添加 Recipe 示例说明。

---

## Group 4 — 安全边界

4.1 添加 threat model。

4.2 添加已知限制和 Backlog。

4.3 确保所有示例不包含真实 secret。

---

## Group 5 — 验证

5.1 跑 `pnpm install`。

5.2 跑 `pnpm build`。

5.3 跑 `pnpm typecheck`。

5.4 跑 `pnpm test`。

5.5 跑 `pnpm lint`。

5.6 检查 README/docs/recipes 中不含 secret-like 示例。
