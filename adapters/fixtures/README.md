# Adapter Fixtures

This directory is reserved for adapter fixtures used by resolver and provider tests.

Phase 1 only creates the fixture location. Real adapter schema, risk rules, and provider behavior are defined in later phases.
